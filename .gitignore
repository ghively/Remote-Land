# Brainstorming session files (visual companion server)
.superpowers/

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Backend config (contains API key — never commit)
backend/config.json
.env
.env.*

# Node dependencies
node_modules/
backend/node_modules/

# Test coverage output
coverage/

# Local editor / AI-agent tooling state (not project config)
.claude/
.vscode/

# Note: frontend/dist/ is intentionally tracked — this project has no
# build step for end users; the compiled bundles are the deliverable.
# See CLAUDE.md.
